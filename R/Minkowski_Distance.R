Minkowski_distance <- function(v1,v2,p){
  #Check that the p value is a viable input (greater than zero)

  #Check that the lengths of v1 and v2 are compatible

  #Calculate Minkowski distance between the two vectors
  # \sum(|a-b|^p)^(1/p)

}















